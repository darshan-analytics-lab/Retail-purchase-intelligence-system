"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
function getAPIUrl(path, parameters) {
    /**
     * To ensure a safe URL encoding, we always need to place the url parameter at the very end (if exists).
     */
    var url = parameters.url, rest = __rest(parameters, ["url"]);
    var paramString = Object.entries(__assign(__assign({}, rest), { scraper_sdk: 'node2' })).map(function (_a) {
        var key = _a[0], value = _a[1];
        return "".concat(key, "=").concat(encodeURIComponent((value !== null && value !== void 0 ? value : '')));
    }).join('&');
    if (url !== undefined) {
        paramString = "".concat(paramString, "&url=").concat(encodeURIComponent(url.trim()));
    }
    return "https://api.scraperapi.com".concat(path.startsWith('/') ? '' : '/').concat(path, "?").concat(paramString);
}
exports.default = getAPIUrl;
//# sourceMappingURL=getAPIUrl.js.map